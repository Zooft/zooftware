= content_box :headline => @site.name do |box|
  = box.navigation do |n|
    - n.item "List", [:admin, :sites]
    - n.item "Add new", [:new, :admin, :site], :active => true
  
  = render :partial=>"form"
  
  %p
    = link_to "list", [:admin, :sites]

